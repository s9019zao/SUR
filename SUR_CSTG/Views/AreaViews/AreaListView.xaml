<UserControl x:Class="SUR_CSTG.Views.AreaViews.AreaListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid ItemsSource="{Binding Path=Areas, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  ScrollViewer.CanContentScroll="True" 
                  ScrollViewer.HorizontalScrollBarVisibility="Auto" 
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  SelectedItem="{Binding Path=SelectedArea}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id Rejonu" Binding="{Binding Path=AreaId}" />
                <DataGridTextColumn Header="Nazwa Rejonu" Binding="{Binding Path=Name}" />
                <DataGridTextColumn Header="Opis Rejonu" Binding="{Binding Path=Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
